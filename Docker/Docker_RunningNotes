
DATE: 4 MAY 2026

Docker :
Docker is a containerization tool that allows you to package an application along with all its dependencies (code, libraries, runtime, system tools) into a single unit.

It helps create, deploy, and run applications consistently across different environments.

Docker is lightweight, portable, and open-source.

======================
What is a Docker Image?

A Docker image is a read-only template used to create containers.

It includes: 
   Application code   
   Runtime (e.g., Java, Python)   
   Libraries and dependencies   
   Configuration files
   
Images are static and can be stored in registries (like Docker Hub / ECR / GCR ..) 
======================
What is a Docker Container?

     A Docker container is a running instance of a Docker image.     
     It is lightweight and isolated, but shares the host OS kernel.     
     A container includes everything needed to run the application, providing consistency across environments.
	 
======================	 
	 
4. Docker Architecture

Docker architecture consists of these key components:

      Docker Client – Command-line interface to communicate with Docker.
      
      Docker Daemon – Background service that manages containers and images.
      
      Docker Images – Read-only templates to create containers.
      
      Docker Containers – Running instances of images.
      
      Docker Registry – Stores and distributes Docker images (e.g., Docker Hub/ecr/gcr).


====================================


docker -v  
docker --version  
docker info  
docker images  
docker pull <iname>  
docker run amazonlinx -- container will be created and exited immediately
  
docker run -it <iid/imagename> --- enters into container  
docker run -it --name <contname> <iid/imagename> 
exit  --> container gets stoped 
ctrl + p + q 
============



docker ps  -a 
docker ps ( only running containers )  
docker start cid/cname
docker stop cid/cname
docker pause cid/cname
docker unpause cid/cname

docker rm cid/cname

docker rm -f cid/cname

=============================================

docker attach cid --> 
This command attaches your terminal to the running container’s primary process (PID 1). 
It connects you to the standard input (STDIN), output (STDOUT), and error (STDERR) 
It doesn’t start a new process or give you a shell by default. 


docker exec -it <cid/cname> /bin/bash  

This command executes a new process (/bin/bash, a shell) inside the running container and 
connects your terminal to it interactively 
Starts a new shell session inside the container, independent of the main process. 


====================================================
docker rmi iid  
docker rmi iid  --force  
docker rmi -f iid   

========================================
docker run -itd <iid/imagename>  ----------------- 
docker commit <cid> 
docker tag iid <reponame>:<tag> 

docekr rm $(docker ps -aq)  
docker rmi $(docker images -q)  ------ 

======================



root@dockerhost opt]# history
    1  docker -v
    2  yum install docker -y
    3  docker -v
    4  systemctl start docker
    5  systemctl status docker
    6  docker info
    7  cd /var/lib/docker/
    8  ll
    9  cd /opt/
   10  ll
   11  docker images
   12  docker search amazonlinux
   13  docker images
   14  docker pull amazonlinux
   15  docker images
   16  docker pull ubuntu
   17  docker images
   18  docker pull centos
   19  docker pull tomcat
   20  docker images
   21  docker run amazonlinux
   22  docker ps -a
   23  docker run ubuntu
   24  docker ps -a
   25  docker run -it amazonlinux
   26  docker ps -a
   27  docker run -it --name cont1 amazonlinux
   28  docker ps -a
   29  docker ps
   30  docker ps -a
   31  docker rename quizzical_tu cont2
   32  docker ps -a
   33  docker rename optimistic_wright cont3
   34  docker ps
   35  docker ps -a
   36  docker start cont2
   37  docker ps -a
   38  docker start cont3
   39  docker ps -a
   40  docker pause cont1
   41  docker ps -a
   42  docker unpause cont1
   43  docker ps -a
   44  docker attach cont1
   45  docker ps -a
   46  docker start cont1
   47  docker ps -a
   48  docker exec -it cont1 /bin/bash
   49  docker ps -a
   50  docker exec -it cont1 /bin/bash
   51  docker ps -a
   52  docker stop f073d59ecc22
   53  docker ps -a
   54  docker rm cont1
   55  docker ps -a
   56  docker rm cont2
   57  docker rm -f cont2
   58  docker ps -a
   59  docker images
   60  docker rmi0b1ebe5dd426
   61  docker rmi 0b1ebe5dd426
   62  docker rmi -f 0b1ebe5dd426
   63  docker images
   64  docker ps -a
   65  docker ps -aq
   66  docker rm -f $(docker ps -aq)
   67  docker images
   68  docker images -q
   69  docker rmi $(docker images -q)
   70  docker images
   71  docker ps -a
   72  docker run -it --name cont1 amazonlinux
   73  docker ps
   74  docker run -it --name cont2 amazonlinux
   75  docker ps
   76  docker commit cont1
   77  docker images
   78  docker run -it --name cont3 b12039e94c4a
   79  docker run -it --name cont4 b12039e94c4a
   80  docker images
   81  docker tag b12039e94c4a amazonlinux:git
   82  docker imags
   83  docker images
   84  history
====================================================



DATE: 4 MAY 2026

Docker :
Docker is a containerization tool that allows you to package an application along with all its dependencies (code, libraries, runtime, system tools) into a single unit.

It helps create, deploy, and run applications consistently across different environments.

Docker is lightweight, portable, and open-source.

======================
What is a Docker Image?

A Docker image is a read-only template used to create containers.

It includes: 
   Application code   
   Runtime (e.g., Java, Python)   
   Libraries and dependencies   
   Configuration files
   
Images are static and can be stored in registries (like Docker Hub / ECR / GCR ..) 
======================
What is a Docker Container?

     A Docker container is a running instance of a Docker image.     
     It is lightweight and isolated, but shares the host OS kernel.     
     A container includes everything needed to run the application, providing consistency across environments.
	 
======================	 
	 
4. Docker Architecture

Docker architecture consists of these key components:

      Docker Client – Command-line interface to communicate with Docker.
      
      Docker Daemon – Background service that manages containers and images.
      
      Docker Images – Read-only templates to create containers.
      
      Docker Containers – Running instances of images.
      
      Docker Registry – Stores and distributes Docker images (e.g., Docker Hub/ecr/gcr).


====================================


docker -v  
docker --version  
docker info  
docker images  
docker pull <iname>  
docker run amazonlinx -- container will be created and exited immediately
  
docker run -it <iid/imagename> --- enters into container  
docker run -it --name <contname> <iid/imagename> 
exit  --> container gets stoped 
ctrl + p + q 
============



docker ps  -a 
docker ps ( only running containers )  
docker start cid/cname
docker stop cid/cname
docker pause cid/cname
docker unpause cid/cname

docker rm cid/cname

docker rm -f cid/cname

=============================================

docker attach cid --> 
This command attaches your terminal to the running container’s primary process (PID 1). 
It connects you to the standard input (STDIN), output (STDOUT), and error (STDERR) 
It doesn’t start a new process or give you a shell by default. 


docker exec -it <cid/cname> /bin/bash  

This command executes a new process (/bin/bash, a shell) inside the running container and 
connects your terminal to it interactively 
Starts a new shell session inside the container, independent of the main process. 


====================================================
docker rmi iid  
docker rmi iid  --force  
docker rmi -f iid   

========================================
docker run -itd <iid/imagename>  ----------------- 
docker commit <cid> 
docker tag iid <reponame>:<tag> 

docekr rm $(docker ps -aq)  
docker rmi $(docker images -q)  ------ 

======================



root@dockerhost opt]# history
    1  docker -v
    2  yum install docker -y
    3  docker -v
    4  systemctl start docker
    5  systemctl status docker
    6  docker info
    7  cd /var/lib/docker/
    8  ll
    9  cd /opt/
   10  ll
   11  docker images
   12  docker search amazonlinux
   13  docker images
   14  docker pull amazonlinux
   15  docker images
   16  docker pull ubuntu
   17  docker images
   18  docker pull centos
   19  docker pull tomcat
   20  docker images
   21  docker run amazonlinux
   22  docker ps -a
   23  docker run ubuntu
   24  docker ps -a
   25  docker run -it amazonlinux
   26  docker ps -a
   27  docker run -it --name cont1 amazonlinux
   28  docker ps -a
   29  docker ps
   30  docker ps -a
   31  docker rename quizzical_tu cont2
   32  docker ps -a
   33  docker rename optimistic_wright cont3
   34  docker ps
   35  docker ps -a
   36  docker start cont2
   37  docker ps -a
   38  docker start cont3
   39  docker ps -a
   40  docker pause cont1
   41  docker ps -a
   42  docker unpause cont1
   43  docker ps -a
   44  docker attach cont1
   45  docker ps -a
   46  docker start cont1
   47  docker ps -a
   48  docker exec -it cont1 /bin/bash
   49  docker ps -a
   50  docker exec -it cont1 /bin/bash
   51  docker ps -a
   52  docker stop f073d59ecc22
   53  docker ps -a
   54  docker rm cont1
   55  docker ps -a
   56  docker rm cont2
   57  docker rm -f cont2
   58  docker ps -a
   59  docker images
   60  docker rmi0b1ebe5dd426
   61  docker rmi 0b1ebe5dd426
   62  docker rmi -f 0b1ebe5dd426
   63  docker images
   64  docker ps -a
   65  docker ps -aq
   66  docker rm -f $(docker ps -aq)
   67  docker images
   68  docker images -q
   69  docker rmi $(docker images -q)
   70  docker images
   71  docker ps -a
   72  docker run -it --name cont1 amazonlinux
   73  docker ps
   74  docker run -it --name cont2 amazonlinux
   75  docker ps
   76  docker commit cont1
   77  docker images
   78  docker run -it --name cont3 b12039e94c4a
   79  docker run -it --name cont4 b12039e94c4a
   80  docker images
   81  docker tag b12039e94c4a amazonlinux:git
   82  docker imags
   83  docker images
   84  history

========================================


DATE :  5 MAY 2026 


docker hub account creation  
 
creation of Imagerepo(batch17d) in your DH account  
 
docker image push to  - Image repo in your DH account  



docker push devopshubg333/batch17d:tagname
docker login 

uname 
passwoerd 

docker pull devopshubg333/batch16d:gitjava

 
======================================= 
A SMPLE HTTPD WEB APPLICATION SET UP :

MANULLY :

httpd : 80 
yum install httpd -y 
systemctl start httpd 
cd /var/www/html  ----> index.html 
IP:80 
==========================================

DOCKERIZING APPLICATION: 
 
port maping /port forwards  
 
docker run -it --name <contname> -p HP:CP amazonlinux  
 
docker run -it --name fb_httpd_cont2 -p 8081:80 amazonlinux 
 
yum install httpd -y  
cd /usr/sbin/ 
./httpd  
cd /var/www/html/ 
yum install vim -y  
vim index.html  


==============================================
docker cp src dest  
docker cp index.html fb_httpd_cont2:/var/www/html/index.html 
Successfully copied 77.8kB to fb_httpd_cont2:/var/www/html/index.html 

======================

root@dockerhost opt]# history
    1  docker -v
    2  yum install docker -y
    3  docker -v
    4  systemctl start docker
    5  systemctl status docker
    6  docker info
    7  cd /var/lib/docker/
    8  ll
    9  cd /opt/
   10  ll
   11  docker images
   12  docker search amazonlinux
   13  docker images
   14  docker pull amazonlinux
   15  docker images
   16  docker pull ubuntu
   17  docker images
   18  docker pull centos
   19  docker pull tomcat
   20  docker images
   21  docker run amazonlinux
   22  docker ps -a
   23  docker run ubuntu
   24  docker ps -a
   25  docker run -it amazonlinux
   26  docker ps -a
   27  docker run -it --name cont1 amazonlinux
   28  docker ps -a
   29  docker ps
   30  docker ps -a
   31  docker rename quizzical_tu cont2
   32  docker ps -a
   33  docker rename optimistic_wright cont3
   34  docker ps
   35  docker ps -a
   36  docker start cont2
   37  docker ps -a
   38  docker start cont3
   39  docker ps -a
   40  docker pause cont1
   41  docker ps -a
   42  docker unpause cont1
   43  docker ps -a
   44  docker attach cont1
   45  docker ps -a
   46  docker start cont1
   47  docker ps -a
   48  docker exec -it cont1 /bin/bash
   49  docker ps -a
   50  docker exec -it cont1 /bin/bash
   51  docker ps -a
   52  docker stop f073d59ecc22
   53  docker ps -a
   54  docker rm cont1
   55  docker ps -a
   56  docker rm cont2
   57  docker rm -f cont2
   58  docker ps -a
   59  docker images
   60  docker rmi0b1ebe5dd426
   61  docker rmi 0b1ebe5dd426
   62  docker rmi -f 0b1ebe5dd426
   63  docker images
   64  docker ps -a
   65  docker ps -aq
   66  docker rm -f $(docker ps -aq)
   67  docker images
   68  docker images -q
   69  docker rmi $(docker images -q)
   70  docker images
   71  docker ps -a
   72  docker run -it --name cont1 amazonlinux
   73  docker ps
   74  docker run -it --name cont2 amazonlinux
   75  docker ps
   76  docker commit cont1
   77  docker images
   78  docker run -it --name cont3 b12039e94c4a
   79  docker run -it --name cont4 b12039e94c4a
   80  docker images
   81  docker tag b12039e94c4a amazonlinux:git
   82  docker imags
   83  docker images
   84  history
   85  hostnamectl set-hostname dockerhost
   86  sudo su -
   87  systemctl status docker
   88  docker imags
   89  docker images
   90  docker push devopshubg333/testrepo:tagname
   91  docker push amazonlinux:git
   92  docker tag b12039e94c4a devopshubg333/testrepo:git
   93  docker images
   94  docker push devopshubg333/testrepo:git
   95  docker login
   96  docker login
   97  docker push devopshubg333/testrepo:git
   98  yum install httpd -y
   99  cd /var/www/html/
  100  ll
  101  vim index.html
  102  docker run -it --name h-c1 -p 8081:80 amazonlinux
  103  ll
  104  cd /var/www/html/
  105  l
  106  ll
  107  cd /opt/
  108  ll
  109  vim index.html
  110  cd /opt/
  111  vim index.html
  112  docker images
  113  docker ps
  114  vim index.html
  115  docke cp index.html f6a0610735c3:/var/www/html/index.htmk
  116  docker cp index.html f6a0610735c3:/var/www/html/index.htmk
  117  docker cp index.html f6a0610735c3:/var/www/html/index.html
  118  docker ps
  119  docker run -itd --name ct1 amazonlinux sleep 10
  120  docker ps -a
  121  docker run -itd --name ct1 amazonlinux ping www.google.com
  122  docker run -it --name ct2 amazonlinux ping www.google.com
  123  env
  124  docker run -it amazonlinux bash -c "yum install -y iputils && ping www.google.com"
  125  docker images
  126  systemctl status docker
  127  docker images
  128  docker ps -a
  129  docker ps -aq
  130  docker rm -f $(docker ps -aq)
  131  docker images -q
  132  docker rmi -f $(docker images -q)
  133  docker images
  134  docker ps -a
  135  docker run -it --name cont1 amazonlix
  136  docker run -it --name cont1 amazonlinux
  137  docker ps -a
  138  docker commit cont1
  139  docker images
  140  docker tag 60c758a52a1b amazonlinux:gitjava
  141  docker images
  142  docker run -it --name sbx amazonlinux:gitjava
  143  docker run -it --name prod amazonlinux:gitjava
  144  docker ps -a
  145  docker attach prod
  146  docker ps -a
  147  docker exec -it sbx /bin/bash
  148  docker ps -a
  149  docker images
  150  docker login
  151  docker push amazonlinux:gitjava
  152  docker tag 60c758a52a1b devopshubg333/batch17d:git_java
  153  docker images
  154  docker push devopshubg333/batch17d:git_java
  155  docker ps
  156  docker run -it --name cont2 amazonlinux sleep 15
  157  docker ps -a
  158  docker run -itd --name cont3 amazonlinux sleep15
  159  docker run -itd --name cont4 amazonlinux sleep 15
  160  docker ps
  161  docker ps -a
  162  docker exec -it sbx /bin/bash
  163  docker run -itd --name cont6 amazonlinux ping www.google.com
  164  docker ps -a
  165  docker -v
  166  git
  167  java
  168  yum install httpd -y
  169  systemctl start httpd
  170  systemctl status httpd
  171  cd /var/www/html/
  172  vim index.html
  173  docker run -it --name httpd_ct1 amazonlinux
  174  docker ps
  175  docker run -it --name httpd_ct2 -p 8085:80  amazonlinux
  176  docker ps
  177  docker commit httpd_ct2
  178  docker images
  179  docker tag 47c2a447ddba  devopshubg333/batch17d:httpd
  180  docker images
  181  docker push devopshubg333/batch17d:httpd
  182  docker rm -f $(docker ps -qa)
  183  docker rmi -f $(docker images -q)
  184  docker images
  185  docker pull devopshubg333/batch17d:httpd
  186  docker images
  187  docker run -it --name cont1 -p 8080:80 devopshubg333/batch17d:httpd
  188  systemctl enable docker
  189  docker ps
  190  ll
  191  cd /opt/
  192  ll
  193  vim index.html
  194  docker pa
  195  docker ps
  196  docker cp index.html cont1:/var/www/html/index.html
  197  history




DATE:  6 MAY 2026


docker history imagename:tag


docker logs cid 
docker logs -f cont-id
docker logs --tail 100 cont-id
docker logs --since 10m cont-id

------------------

docker home path --> /var/lib/docker 
---------

Docker Container - ephemeral nature : 

Docker containers are ephemeral by design, meaning they are temporary and stateless. Once a container stops or is deleted, any data or changes made inside it during its runtime are lost unless explicitly preserved 

DOCKER VOLUMES  
docker volume create my_volume 


docker run -it -v HVolume:/opt/prod_logs image

docker run -d -v my_volume:/container/path   my_image 


Usage:  docker volume COMMAND

Manage volumes

Commands:
  create      Create a volume
  inspect     Display detailed information on one or more volumes
  ls          List volumes
  prune       Remove unused local volumes
  rm          Remove one or more volumes
  update      Update a volume (cluster volumes only)

--------------------------------- 
BIND MOUNTS  
docker run -v /host/path:/container/path my-image 
docker run -it --name cont2 -v /opt:/opt/logs iname 

============================================



====================================================================
DOCKER VOLUMES 
====================================================================

--------------------------------------------------
WHAT IS A DOCKER VOLUME?
--------------------------------------------------

    Docker Volume is a storage mechanism
    used to persist container data.

    By default:

        Container data is temporary.

    If container is deleted:

        Data is also deleted ❌

    Volumes solve this problem ✅


--------------------------------------------------
WHY DO WE NEED VOLUMES?
--------------------------------------------------

    Containers are ephemeral (temporary).

    Without volume:

        Container deleted → Data lost

    With volume:

        Data survives even if container is removed


--------------------------------------------------
SIMPLE USE CASE
--------------------------------------------------

    Scenario:

        MySQL container stores database data.

    Without volume:

        Container removed → Database lost ❌

    With volume:

        Data stored outside container ✅


--------------------------------------------------
HOW VOLUMES WORK
--------------------------------------------------

    Container
        ↓
    Mounted volume
        ↓
    Data stored on host machine


--------------------------------------------------
TYPES OF DOCKER STORAGE
--------------------------------------------------

    1) Volumes
    2) Bind Mounts
	3) 


--------------------------------------------------
1) DOCKER VOLUMES (RECOMMENDED)
--------------------------------------------------

    Managed by Docker.

    Stored in:

        /var/lib/docker/volumes/

Example:

    docker volume create myvolume

Run container:

    docker run -d  -v myvolume:/app/data  nginx


--------------------------------------------------
2) BIND MOUNTS
--------------------------------------------------

    Maps host directory directly  into container.

Example:

    docker run -d -v /opt/logs:/opt/los amazonlinux

    Left side:
        Host path

    Right side:
        Container path


--------------------------------------------------
3) TMPFS MOUNTS
--------------------------------------------------

    Temporary in-memory storage.

    Data lost after container stops.


--------------------------------------------------
IMPORTANT DOCKER VOLUME COMMANDS
--------------------------------------------------

Create volume:

    docker volume create myvolume

List volumes:

    docker volume ls

Inspect volume:

    docker volume inspect myvolume

Remove volume:

    docker volume rm myvolume


--------------------------------------------------
REAL DEVOPS USE CASES
--------------------------------------------------

    • Database persistence
    • Jenkins home directory
    • Shared logs
    • Application uploads
    • Persistent configs


--------------------------------------------------
JENKINS EXAMPLE
--------------------------------------------------

    docker run -d -p 8080:8080 -v jenkisdata:/var/jenkins_home  jenkins/jenkins:lts

    Jenkins data remains safe even
    if container restarts.


--------------------------------------------------
VOLUME VS BIND MOUNT
--------------------------------------------------

    Volume:

        • Managed by Docker
        • More portable
        • Recommended

    Bind Mount:

        • Direct host path
        • More control
        • Host dependent


--------------------------------------------------
ADVANTAGES OF VOLUMES
--------------------------------------------------

    • Persistent storage
    • Data survives container deletion
    • Easy backup
    • Share data between containers


--------------------------------------------------
SIMPLE SUMMARY
--------------------------------------------------

    Docker Volume = Persistent container storage

    Purpose:
        Keep data safe outside container

=================================




DATE 7 MAY 2026
=============================================== 
Docker file : 
 
how to build --? 
docker build -t <devopshubg333/batch17d:tag> -f Dockerfile .

 ---------------------------------------- 
Dockerfile for httpd :

manual :

amazonlinux 
yum install httpd -y 
/usr/sbin/httpd 
/var/www/html/index.html 
=============================

Dockerfile: 


FROM amazonlinux:latest
RUN yum install httpd -y 
COPY index.html /var/www/html/index.html
CMD ["/usr/sbin/httpd", "-D", "FOREGROUND"]
EXPOSE 80

=================
 docker build -t testimg:v1 -f Dockerfile .

 docker tag 2606648e5ff3 devopshubg333/batch17d:httpd



----------------- 
docker run -it -p 8090:80 --name cont1 iid  


========================================================================= 
Dockerizing sample python web-application : 
====== 
Code : /opt/app.py 

import os
from flask import Flask

app = Flask(__name__)

@app.route("/")
def main():
    return "Welcome to Batch17d!"

@app.route("/how are you")
def hello():
    return "I am good, how about you?"

if __name__ == "__main__":
    app.run()


============== 
Commands : 


yum update -y 
yum install python3 python3-pip pip -y 
pip install flask 
FLASK_APP=/opt/python_app/app.py flask run --host=0.0.0.0 --port=8080 


Dockerfile

FROM amazonlinux
RUN yum update -y 
RUN yum install python3 python3-pip pip -y 
RUN pip install flask 
COPY app.py /opt/python_app/app.py
CMD FLASK_APP=/opt/python_app/app.py flask run --host=0.0.0.0 --port=8080 
EXPOSE 8080

----------------- 
docker build -t pythonimg:v1 -f Dockerfile .
docker tag 94b3631fb8d0 devopshubg333/batch17d:pythonimg_v1

==============================================================================================


date: 8 MAY 2026 


-----------------------------------------------

All these days amazonlinux 

use other images  like ubuntu, centos ...etc 

export PS1='\u@\h# '


docker run -it -h <hostname>  iname 

-----------------------------------------------
FROM ubuntu:latest

RUN apt-get update && \
    apt-get install -y python3 python3-pip && \
    apt-get clean

COPY app.py /opt/python_app/app.py

RUN pip3 install flask --break-system-packages

EXPOSE 8080

CMD flask --app /opt/python_app/app.py run --host=0.0.0.0 --port=8080

======================
# use base image python 
FROM python:3.12-slim

LABEL maintainer="Madhukiran <madhu@gmail.com>"

WORKDIR /opt/python_app

COPY app.py .

RUN pip install flask

EXPOSE 8080

CMD ["flask", "--app", "app.py", "run", "--host=0.0.0.0", "--port=8080"]


----------------------------------------------

DOCKERFILE DEEP DIVE: 

FROM
RUN
COPY
CMD
EXPOSE 
LABEL
ADD 
WORKDIR 
ADD 
COPY VS ADD 
----------

ENTRYPOINT 
CMD VS ENTRYPOINT 

===========
Dockerfile Instructions Simplified

RUN

    Executes commands while building the image.    
    Creates a new image layer.    
    Commonly used to install software packages
	it is basically during Image Building

CMD
    DURING CONTAINER CREATIING  -
    Sets the default command when the container starts.    
    Can be overridden by arguments passed to docker run.    


ENTRYPOINT
      DURING CONTAINER CREATIING  -
      Configures the container to run as an executable.      
      Cannot be fully overridden, but command-line arguments are appended.
	  GIven High priority 

=====================



======================================= 
docker system prune 
docker container prune 
docker image prune 
docker volume prune  

==========================================================================

DATE: 9 MAY 2026

MANUAL TOMCATSET UP:

sudo yum install java -y
cd /opt
sudo wget https://dlcdn.apache.org/tomcat/tomcat-9/v9.0.117/bin/apache-tomcat-9.0.117.tar.gz
sudo tar -xvf /opt/apache-tomcat-9.0.117.tar.gz

cd /opt/apache-tomcat-9.0.117/webapps/manager/META-INF

sed -i '/<Context/,/<\/Context>/c\<Context antiResourceLocking="false" privileged="true" >\n</Context>' context.xml


cd /opt/apache-tomcat-9.0.117/conf
sudo mv tomcat-users.xml tomcat-users_bkup_21apr2026.xml
sudo touch tomcat-users.xml
sudo echo '<?xml version="1.0" encoding="utf-8"?>
        <tomcat-users>
        <role rolename="manager-gui"/>
        <user username="tomcat" password="tomcat" roles="manager-gui, manager-script, manager-status"/>
        </tomcat-users>' > tomcat-users.xml

cd /opt/apache-tomcat-9.0.117/conf/
sudo sed -i 's/Connector port="8080"/Connector port="8081"/g' server.xml
sudo /opt/apache-tomcat-9.0.117/bin/startup.sh
-----------------------------------
TOMCAT DOCKER IMAGE SETUP 

Dockerfile:

FROM amazonlinux:latest
LABEL maintainer="Madhukiran <madhu@gmail.com>"
RUN yum install java wget tar gzip -y && \
    yum clean all 

WORKDIR /opt
RUN wget https://dlcdn.apache.org/tomcat/tomcat-9/v9.0.117/bin/apache-tomcat-9.0.117.tar.gz && \
    tar -xvf /opt/apache-tomcat-9.0.117.tar.gz && \
	rm -rf apache-tomcat-9.0.117.tar.gz
RUN sed -i '/<Context/,/<\/Context>/c\<Context antiResourceLocking="false" privileged="true" >\n</Context>' /opt/apache-tomcat-9.0.117/webapps/manager/META-INF/context.xml

WORKDIR /opt/apache-tomcat-9.0.117/conf
RUN mv tomcat-users.xml tomcat-users_bkup_9May2026.xml && \
    echo '<?xml version="1.0" encoding="utf-8"?>  \
        <tomcat-users> \
        <role rolename="manager-gui"/> \
        <user username="tomcat" password="tomcat" roles="manager-gui, manager-script, manager-status"/> \
        </tomcat-users>' > tomcat-users.xml   && \

    sed -i 's/Connector port="8080"/Connector port="8081"/g' server.xml

CMD ["/opt/apache-tomcat-9.0.117/bin/catalina.sh", "run" ]
EXPOSE 8081

====

docker build -t devopshubg333/batch17d:tomcat -f Dockerfile .


docker images
REPOSITORY               TAG       IMAGE ID       CREATED          SIZE
devopshubg333/batch17d   tomcat    1b745cdf89f4   15 seconds ago   531MB



[root@dockerhost tomcatsetup]# docker history 1b745cdf89f4
IMAGE          CREATED          CREATED BY                                      SIZE      COMMENT
1b745cdf89f4   52 seconds ago   EXPOSE map[8081/tcp:{}]                         0B        buildkit.dockerfile.v0
<missing>      52 seconds ago   CMD ["/opt/apache-tomcat-9.0.117/bin/catalin…   0B        buildkit.dockerfile.v0
<missing>      52 seconds ago   RUN /bin/sh -c mv tomcat-users.xml tomcat-us…   11kB      buildkit.dockerfile.v0
<missing>      52 seconds ago   WORKDIR /opt/apache-tomcat-9.0.117/conf         0B        buildkit.dockerfile.v0
<missing>      52 seconds ago   RUN /bin/sh -c sed -i '/<Context/,/<\/Contex…   895B      buildkit.dockerfile.v0
<missing>      52 seconds ago   RUN /bin/sh -c wget https://dlcdn.apache.org…   18.4MB    buildkit.dockerfile.v0
<missing>      54 seconds ago   WORKDIR /opt                                    0B        buildkit.dockerfile.v0
<missing>      54 seconds ago   RUN /bin/sh -c yum install java wget tar gzi…   363MB     buildkit.dockerfile.v0
<missing>      54 seconds ago   LABEL maintainer=Madhukiran <madhu@gmail.com>   0B        buildkit.dockerfile.v0
<missing>      15 hours ago     CMD ["/bin/bash"]                               0B        buildkit.dockerfile.v0
<missing>      15 hours ago     COPY /rootfs/ / # buildkit                      150MB     buildkit.dockerfile.v0



[root@dockerhost tomcatsetup]#

docker run -it --name tomcat_ct1 -p 8088:8081 devopshubg333/batch17d:tomcat

 docker login 
 docker push devopshubg333/batch17d:tomcat




docker pull jenkins/jenkins:lts

docker run -it --name jenkins_ct -p 8080:8080 1cea533038c6




----------------------------------

JENKINS FILE :



pipeline {
    agent any
	
	tools {
	  maven 'Maven_3.9.15'
	}

    stages {
	
        stage('checkout') {
            steps {
                echo 'git checkout stage'
				git branch: 'main', url: 'https://github.com/devopstraininghub/mindcircuit17d.git'
            }
        }

        stage('build') {
            steps {
                echo 'Building with maven '
				sh 'mvn clean install '
            }
        }

        stage('Deploy') {
            steps {
                echo 'Deploying to tomcat'
	            deploy adapters: [tomcat9(alternativeDeploymentContext: '', credentialsId: 'tomcat', path: '', url: 'http://54.161.66.25:8088/')], contextPath: 'facebook', war: '**/*.war'
				
            }
        }		
		
    }
}
==================

DATE: 11 MAY 2026

MULTI STAGE DOCKER FILE :

=====================================
mindcircuit17d AS A IMAGE (USING MULTI STAGE DOCKERFILE) 



FROM maven AS buildstage 
RUN mkdir /opt/mindcircuit16d
WORKDIR /opt/mindcircuit16d
COPY . .
mvn clean install 


FROM tomcat 
WORKDIR webapps
COPY --from=buildstage /opt/mindcircuit16d/target/*.war .
RUN rm -rf ROOT && mv *.war ROOT.war
EXPOSE 8080

=========================================
DATE: 15 MAY 2026

CALCULATOR APP 

calculator.go  
========= 

 
package main

import (
	"bufio"
	"fmt"
	"os"
	"strconv"
	"strings"
)

func main() {
	fmt.Println("HELLO BATCH17-d AWS DEVOPS CHAMPS, I am a calculator app ....")

	for {
		// Read input from the user
		reader := bufio.NewReader(os.Stdin)
		fmt.Print("Enter any calculation (Example: 1 + 2 OR 2 * 5) \n(Type 'exit' to quit): ")
		text, _ := reader.ReadString('\n')

		// Trim the newline character from the input
		text = strings.TrimSpace(text)

		// Check if the user entered "exit" to quit the program
		if text == "exit" {
			fmt.Println("Exiting... Goodbye! 👋")
			break
		}

		// Split the input into three parts: left operand, operator, right operand
		parts := strings.Split(text, " ")
		if len(parts) != 3 {
			fmt.Println("Invalid input. Try again. (Example: 3 + 4)")
			continue
		}

		// Convert the operands to integers
		left, err := strconv.Atoi(parts[0])
		if err != nil {
			fmt.Println("Invalid number:", parts[0])
			continue
		}
		right, err := strconv.Atoi(parts[2])
		if err != nil {
			fmt.Println("Invalid number:", parts[2])
			continue
		}

		// Perform the calculation based on the operator
		var result int
		switch parts[1] {
		case "+":
			result = left + right
		case "-":
			result = left - right
		case "*":
			result = left * right
		case "/":
			if right == 0 {
				fmt.Println("Error: Division by zero is not allowed.")
				continue
			}
			result = left / right
		default:
			fmt.Println("Invalid operator. Use +, -, *, or /")
			continue
		}

		// Print the result
		fmt.Printf("Result: %d\n", result)
	}
}

 
 
 
===================== 
MANUAL STEPS: ---
 
yum update  
yum install -y go 
GO111MODULE=off 
go build calculator.go 


====================================== 
[root@ip-172-31-40-108 opt]# cat Dockerfile
 
FROM amazonlinux
RUN yum update  && \
    yum install -y go
ENV GO111MODULE=off
WORKDIR /opt
COPY . .
RUN go build calculator.go
CMD ["/opt/calculator"]

********************************************************************** 
MULTI STAGE DOCKER FILE :


FROM amazonlinux AS buildstage
RUN yum update  && \
    yum install -y go 
ENV GO111MODULE=off 
WORKDIR /opt
COPY . .
RUN go build calculator.go 

FROM scratch
COPY --from=buildstage /opt/calculator /opt/calculator
CMD ["/opt/calculator"]


==================================================================


[root@ip-172-31-40-108 opt]# cat Dockerfile.ubuntu 

FROM ubuntu 
RUN apt-get update && apt-get install -y golang-go 
ENV GO111MODULE=off 
WORKDIR /opt/calculatorapp 
COPY . . 
RUN go build calculator.go 
ENTRYPOINT ["/opt/calculatorapp/calculator"] 
================================================== 
[root@ip-172-31-40-108 opt]# cat Dockerfile-multistage 


FROM amazonlinux AS buildstage
RUN yum update  && \
    yum install -y go
ENV GO111MODULE=off
WORKDIR /opt
COPY . .
RUN go build calculator.go

FROM scratch
COPY --from=buildstage  /opt/calculator /opt/calculator
ENTRYPOINT ["/opt/calculator"]

==========================================


DATE: 14 MAY 2026 



DOCKER NETWORKING :

IP address
NIC / ethernet
NETWORK - (VPC IN AWS TERMINOLOGY )  
SUBNET
GATEWAY

======================================

bridge
host 
none 
overlay 

================================
docker network list /ls 
docker network inspect bridge 

docker network create --subnet 10.81.0.0/16 batch17dnw

docker run -it --name ct33 --network batch17dnw --ip 10.81.3.33 ubuntu

docker run -it --name hostnwtct --network host ubuntu

docker run -itd --net batch16nw --ip 10.81.0.20 <iname> 


docker network rm batch16nw 


===================================================
# 1. WHAT IS DOCKER NETWORKING?


Docker Networking allows containers to communicate:
- With each other
- With host machine
- With internet
- With external systems/databases

Without networking:
    Containers are isolated

With networking:
    Containers can communicate

========================================
 TYPES OF DOCKER NETWORKS


1. bridge   -> Default local container communication
2. host     -> Share host network
3. none     -> No networking
4. overlay  -> Multi-Docker host communication
5. macvlan  -> Container gets real LAN IP


=========================================
 BRIDGE NETWORK (MOST COMMON)


Default Docker network.

When container starts:
    docker run nginx

Docker automatically attaches it to:
    bridge network

Example:
    Container A -> 172.17.0.2
    Container B -> 172.17.0.3

Containers communicate internally.


===========================================
 CHECK DOCKER NETWORKS


docker network ls



#  INSPECT NETWORK


docker network inspect bridge



# CREATE CUSTOM NETWORK (BEST PRACTICE)


docker network create mynetwork



# RUN CONTAINER IN CUSTOM NETWORK


docker run -dit --name db --network mynetwork mysql

docker run -dit --name backend --network mynetwork ubuntu



# CONTAINER COMMUNICATION USING NAMES


Containers can communicate using names:
    db
    backend
    redis

Example:
    backend -> db:3306

No need for IP addresses.

===========================================

# HOST NETWORK


Container shares host network directly.

Command:
    docker run --network host nginx

Features:
- No container IP
- Uses host IP directly
- High performance
- Less isolation

======================================
# NONE NETWORK


No networking at all.

Command:
    docker run --network none ubuntu

Container:
- Cannot access internet
- Cannot communicate


=====================================================
#  OVERLAY NETWORK


Used in Docker Swarm / multi-host communication.

Allows containers on different servers
to communicate.

Example:
    Server1 -> frontend
    Server2 -> backend

Overlay network connects both.


==============================================
#  MACVLAN NETWORK


Container gets real LAN IP address.

Example:
    Container IP = 192.168.1.50

Used for:
- Legacy applications
- Network appliances
- Monitoring tools


============================================
#  PORT MAPPING


Expose container application externally.

Command:
    docker run -p 8080:80 nginx

Meaning:
    Host Port 8080 -> Container Port 80

Access:
    http://SERVER-IP:8080


===============================
#  HOW PACKET/TRAFFIC FLOWS


Internet
   ↓
Host Machine
   ↓
Docker Port Mapping
   ↓
Container Application

================================


# 16. IMPORTANT NETWORK COMMANDS


# List networks
docker network ls

# Inspect network
docker network inspect bridge

# Create network
docker network create mynet

# Remove network
docker network rm mynet

# Connect container to network
docker network connect mynet container1

# Disconnect container
docker network disconnect mynet container1

=============================================

 SECURITY BEST PRACTICES


# Use custom bridge networks
# Expose only required ports
# Avoid host network unless needed
# Separate frontend/backend networks
# Do not expose databases publicly


===================================================================
# DEFAULT NETWORKS CREATED BY DOCKER


bridge   -> Default bridge network
host     -> Host networking
none     -> No networking



# CHECK CONTAINER IP ADDRESS


docker inspect container_name

OR

docker exec -it container_name ip a



#  EXAMPLE: FULL APPLICATION NETWORK


# Create network
docker network create appnet

# Run MySQL
docker run -d \
  --name mysql \
  --network appnet \
  mysql

# Run Backend
docker run -d \
  --name backend \
  --network appnet \
  node-app

# Run Frontend
docker run -d \
  --name frontend \
  --network appnet \
  react-app


===========================================================================================


DATE: 15 MAY 2026

SonarQube is a popular open-source platform used for continuous inspection of code quality. It helps developers detect bugs, code smells, and security vulnerabilities in their code before it goes to production.

Purpose of SonarQube

     Code Quality Management: Ensures code follows best practices.
     
     Bug Detection: Finds potential runtime errors early. ( NULL POINTER ) 
	 
     Maintainability: Detects “code smells,” which are parts of the code that are poorly written or hard to maintain.
     
     Security: Identifies vulnerabilities to prevent security issues.
     
     Technical Debt Tracking: Shows how much “debt” your code has (i.e., areas that need refactoring).
	 
	 9000
Key Features

       Multi-language Support: Works with Java, C#, JavaScript, Python, PHP, and many more.
       
       Static Code Analysis: Examines code without running it.
       
       Automated Reports: Generates dashboards and reports to visualize code quality.
       
       Integration: Works with CI/CD tools like Jenkins, GitHub Actions, GitLab CI, etc.
       
       Custom Rules: Allows you to define coding rules specific to your project or organization.
	   
	   Analyze for Issues: It checks for:

         Bugs, Vulnerabilities, Code smells, Duplicated code, Complexity
		 
		 
		 
1 Static Code Analysis

SonarQube scans source code for:

    Category	             Examples
			
    Bugs	                Null pointer, memory leaks (Problems likely to break application.)
    Vulnerabilities	        SQL injection, hardcoded secrets (Security risks) 
    Code Smells	            Duplicate code, long methods (Bad coding practices.) 
    Coverage              	Unit test coverage
    Duplications	        Repeated logic
            
 
Severity Levels 🚨

Each rule has severity:


        Blocker -->	Very dangerous
        Critical -->Serious issue
        Major -->   Important
        Minor -->  	Small issue
        Info	    Suggestion

==================================== 
Quality profiles
Quality profiles are collections of rules to apply during an analysis. For each language, there is a default profile. 



Quality Gate:

        Condition	                   Required
        Coverage	                     > 80%
        Blocker bugs	                  0
        Critical vulnerabilities	      0
        Duplicated code	                 < 5%
 
 ==============================================
 
 
            Quality Profile
      (Rules to Analyze Code)
                    ↓
             SonarQube Scan
                    ↓
          Issues + Metrics Found
                    ↓
             Quality Gate
          (Pass / Fail Decision)
                    ↓
            CI/CD Pipeline
 
 
 
 ========== 
 docker pull sonarqube
 
 docker run -itd --name soanr_ct -p 9000:9000 sonar
 ===========================
docker pull jenkins/jenkins:lts
docker run -itd --name jenkins_ct -p 8080:8080 jenkins/jenkins:lts



to build code :
mvn clean install 

to scan code :

mvn sonar:sonar
    http://54.80.12.156:9000/
    token  -- squ_634ed45086db19f4e38c72ff961a635eb3aae1de
  
  
 

In cicd integration , one of stage:

 
		stage('sonarqube scan') {
            steps {
                echo 'This stage scans the code using sonarqube'
                
                sh ''' mvn sonar:sonar \\
                      -Dsonar.host.url=http://54.80.12.156:9000 \\
                      -Dsonar.login=squ_634ed45086db19f4e38c72ff961a635eb3aae1de'''
            }
    	}	
==================================


PIPELINE 

pipeline {
    agent any
	
	tools {
	
	maven 'Maven_3.9.15'
	}

    stages {
	
        stage('checkout') {
            steps {
                echo 'git checkout stage'
				git branch: 'main', url: 'https://github.com/devopstraininghub/mindcircuit17d.git'
            }
        }

 		stage('sonarqube scan') {
            steps {
                echo 'This stage scans the code using sonarqube'
                
                sh ''' mvn sonar:sonar \\
                      -Dsonar.host.url=http://54.80.12.156:9000 \\
                      -Dsonar.login=squ_634ed45086db19f4e38c72ff961a635eb3aae1de'''
            }
    	}


        stage('build') {
            steps {
                echo 'Building with maven '
				sh 'mvn clean install '
            }
        }

        stage('Deploy') {
            steps {
                echo 'Deploying to tomcat'
	//			deploy adapters: [tomcat9(alternativeDeploymentContext: '', credentialsId: 'tomcat', path: '', url: 'http://54.160.144.88:8081/')], contextPath: 'insta', war: '**/*.war'
				
            }
        }		
		
    }
}

==================================================



DATE: 18 MAY2026


TRIVY :
https://github.com/aquasecurity/trivy/



rpm -ivh  xyz.rpm
rpm -Uvh xyz.rpm

rpm -ivh https://github.com/aquasecurity/trivy/releases/download/v0.70.0/trivy_0.70.0_Linux-64bit.rpm


IMAGE SCANNING using TRIVY 

Trivy is an open-source vulnerability and security scanner created by Aqua Security.

CVE = Common Vulnerabilities and Exposures
 - Scans for vulnerabilities (CVEs) in   
  
            Container images (Docker, OCI)          
            File systems and directories          
            Git repositories          
            Infrastructure as Code (IaC) (Terraform, Kubernetes manifests, Helm charts)                
            Cloud environments
  
 - Detects misconfigurations and sensitive information (secrets, tokens, passwords) in code.
  
 - Supports CVE databases (like NVD, GitHub Security Advisories) and can be updated frequently.
 
 
 
 WGET --> download --> bin / start 

 
 sonarqube --> docker 
 
 RPM : 

  rpm -ivh 

  rpm - Uvh 
-------------------------------
 PIPELINE INTEGRATING TRIVY :
 
 pipeline {
    agent any


     tools { 
        maven 'maven3' 
    }
	
	stages {
	
    stage('Checkout') { 
            steps { 
                echo 'Cloning GIT HUB Repo' 
                // Clone the specified branch from the GitHub repository 
                git branch: 'main', url: 'https://github.com/devopstraininghub/mindcircuit17d.git'
            }   
        } 
	
	
      stage('SonarQube Scan') {
      steps {
             echo 'Scanning project'
            sh 'ls -ltr'
        
        sh '''
            mvn sonar:sonar \
                -Dsonar.host.url=http://13.220.82.203:9000 \
                -Dsonar.login=squ_f21d96bcca2ac0be12229663009aa66157fa8e45
        '''
         }
       }
	
        stage('Build Artifact') { 
            steps { 
                echo 'Build Artifact' 
                sh 'mvn clean package' 
            } 
        } 
	
	
	
      stage('Build Docker Image') { 
            steps { 
                echo 'Build Docker Image' 
 
                sh 'docker build -t devopshubg333/batch17d:${BUILD_NUMBER} -f Dockerfile .' 
            } 
        } 
	
	stage('Scan Docker Image using Trivy') { 
            steps { 
                echo 'scanning Image' 
               
               
                sh 'trivy image devopshubg333/batch17d:${BUILD_NUMBER}' 
            } 
        } 
	
	
	  stage('Push to Docker Hub') { 
            steps { 
                script { 
                 
                  	withCredentials([string(credentialsId: 'dockerhub', variable: 'dockerhub')]) {
                         sh 'docker login -u devopshubg333 -p ${dockerhub}' 
	
                      }
                    // Push the Docker image to Docker Hub 
                    sh 'docker push devopshubg333/batch16d:${BUILD_NUMBER}' 
                    echo 'Pushed to Docker Hub' 
                } 
            } 
        } 
	
	
	
==============================================
SAST


DAST
==============================================
	
	
	
	stage --> deploy on UAT/sbox
	
	   { 
	    }
		
	STAGE -- dast scanning 
	
	 { 
	 
	 } 
	 
	 	
	stage --> deploy on PRODUCTION 
	
	   { 
	    }
	 
	 
	
	
	

==========================================================================
DATE: 22 MAY 2026



22 MAY 2025:

 
Docker compose Installation :

sudo curl -L "https://github.com/docker/compose/releases/download/v2.23.1/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose


sudo chmod +x /usr/local/bin/docker-compose 

docker-compose version 
 
================== 
docker-compose.yml 


==========================================

  docker-compose up -d 
  docker-compose down --volumes 


=====================

NOTES:


--------------------------------------------------
WHAT IS DOCKER COMPOSE?
--------------------------------------------------

    Docker Compose is a tool used to define
    and run multi-container Docker applications.

    Instead of running multiple docker run commands,
    we define everything in one YAML file.

    File name:

        docker-compose.yml
        OR
        compose.yaml


--------------------------------------------------
WHY DO WE NEED DOCKER COMPOSE?
--------------------------------------------------

    Problem without Compose:

        Need multiple commands:

            docker run mysql
            docker run backend
            docker run frontend

        Hard to manage ❌

    Solution:

        Define all containers in one file ✅


--------------------------------------------------
WHAT CAN DOCKER COMPOSE MANAGE?
--------------------------------------------------

    • Multiple containers
    • Networks
    • Volumes
    • Environment variables
    • Ports
    • Dependencies


--------------------------------------------------
REAL-TIME USE CASE
--------------------------------------------------

    Application architecture:

        Frontend container
        Backend container
        Database container

    Docker Compose starts all together.


--------------------------------------------------
HOW DOCKER COMPOSE WORKS
--------------------------------------------------

    compose.yaml
            ↓
    docker compose up
            ↓
    Creates:
        • Containers
        • Networks
        • Volumes


--------------------------------------------------
BASIC STRUCTURE
--------------------------------------------------

    version:
    services:
	  - db 
	  - wordprss 
    volumes:
	   - 1
	   - 2
    networks:
	

--------------------------------------------------
IMPORTANT KEYWORDS
--------------------------------------------------

    services:
        Defines containers

    image:
        Docker image name

    build:
        Build image from Dockerfile

    ports:
        Port mapping

    volumes:
        Persistent storage

    environment:
        Environment variables

    depends_on:
        Container dependency

    networks:
        Communication network


--------------------------------------------------
PORT MAPPING
--------------------------------------------------

Example:

    ports:
      - "8080:80"

Meaning:

    Host port:
        8080

    Container port:
        80


--------------------------------------------------
VOLUMES IN COMPOSE
--------------------------------------------------

Example:

    volumes:
      - mydata:/var/lib/mysql

Purpose:

    Persistent storage


--------------------------------------------------
NETWORKS IN COMPOSE
--------------------------------------------------

    Compose automatically creates network.

    Containers communicate using:

        service names

--------------------------------------------------
DEPENDS_ON
--------------------------------------------------

Example:

    depends_on:
      - db

Meaning:

    Start database before application.



--------------------------------------------------
IMPORTANT COMMANDS
--------------------------------------------------

Start containers:

    docker compose up

Run in background:

    docker compose up -d

Stop containers:

    docker compose down
	
Complete DDeletion:
   docker compose down --volumes 

View containers:

    docker compose ps

View logs:

    docker compose logs  
	docker-complse logs -f

Restart:

    docker compose restart
	
	docker compose ls 


--------------------------------------------------
ADVANTAGES
--------------------------------------------------

    • Easy multi-container management
    • Infrastructure as code
    • Faster setup
    • Easy networking
    • Simple scaling


--------------------------------------------------
DOCKER COMPOSE VS DOCKER RUN
--------------------------------------------------

    docker run:

        • Single container
        • Manual commands

    Docker Compose:

        • Multi-container
        • YAML-based automation


====================================================

Docker Compose helps you:

    Run multiple containers together
    Define services in YAML
    Create local development environments
    Manage networking and volumes

Perfect for:
    local development
    testing
    small projects
    single server apps
	
=============================================================================
DISADVANTAGES:

Compose works mostly on:   ONE machine ( SINGLE DOCKER HOST) 

It lacks advanced production features.

What Happens in Real Production? 🌍

Suppose your app gets:
       1 million users
       traffic spikes
       server failures
       high availability requirements

Now you need:

     ✅ auto scaling
     ✅ self healing
     ✅ load balancing
     ✅ rolling updates
     ✅ zero downtime
     ✅ multi-server deployment
     ✅ monitoring
     ✅ secret management
	 
	 
SOLUTION : GO FOR KUBERNETES / DOCKER-SWARM
	 
====================================================================================

docker-compose.yml (without .env file) 


version: "3.9"
services:
  db:
    image: mysql:5.7
    container_name: wordpress_db
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: somewordpress
      MYSQL_DATABASE: wordpress
      MYSQL_USER: wordpress
      MYSQL_PASSWORD: wordpress
    volumes:
      - db_data:/var/lib/mysql

  wordpress:
    image: wordpress:latest
    container_name: wordpress_app
    depends_on:
      - db
    restart: always
    ports:
      - "8000:80"
    environment:
      WORDPRESS_DB_HOST: db:3306
      WORDPRESS_DB_USER: wordpress
      WORDPRESS_DB_PASSWORD: wordpress
      WORDPRESS_DB_NAME: wordpress
    volumes:
      - wordpress_data:/var/www/html

volumes:
  db_data:
  wordpress_data:
  
networks:
  wordpress_network:
    driver: bridge
    networks:
  wordpress_network:
    driver: bridge
      
===========================================================================
docker-compose.yml (WITH .env file) 



version: "3.9"
services:
  db:
    image: mysql:8.0
    container_name: wordpress_db
    restart: unless-stopped

    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD}
      MYSQL_DATABASE: ${MYSQL_DATABASE}
      MYSQL_USER: ${MYSQL_USER}
      MYSQL_PASSWORD: ${MYSQL_PASSWORD}

    command: --default-authentication-plugin=mysql_native_password

    volumes:
      - db_data:/var/lib/mysql

    networks:
      - wordpress_network

  wordpress:
    image: wordpress:6.5-apache
    container_name: wordpress_app
    restart: unless-stopped

    depends_on:
      - db

    ports:
      - "8000:80"

    environment:
      WORDPRESS_DB_HOST: db:3306
      WORDPRESS_DB_NAME: ${MYSQL_DATABASE}
      WORDPRESS_DB_USER: ${MYSQL_USER}
      WORDPRESS_DB_PASSWORD: ${MYSQL_PASSWORD}

    volumes:
      - wordpress_data:/var/www/html

    networks:
      - wordpress_network

volumes:
  db_data:
  wordpress_data:

networks:
  wordpress_network:
    driver: bridge
    
====================
   
cat .env

MYSQL_ROOT_PASSWORD=StrongRootPassword123
MYSQL_DATABASE=wordpress
MYSQL_USER=wordpress
MYSQL_PASSWORD=StrongUserPassword123


   ========================
